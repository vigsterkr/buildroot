--- argus-2.0.6.fixes.1.old/include/argus_def.h	2004-02-23 16:00:36.000000000 +0100
+++ argus-2.0.6.fixes.1/include/argus_def.h	2008-03-31 16:02:12.000000000 +0200
@@ -56,6 +56,15 @@
 #ifndef Argus_def_h
 #define Argus_def_h
 
+#ifndef bcopy
+#define bcopy(src, dest, n) memmove (dest, src, n)
+#endif
+#ifndef bcmp
+#define bcmp(s1, s2, n) ( n == 0 ? 0 : memcmp (s1, s2, n) )
+#endif
+#ifndef bzero
+#define bzero(s, n) memset (s, 0, n)
+#endif
 
 /*
 Argus Record Header Format
