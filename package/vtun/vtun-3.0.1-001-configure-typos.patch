diff -u vtun-3.0.1.orig1/configure.in vtun-3.0.1/configure.in
--- vtun-3.0.1.orig1/configure.in	2007-10-13 19:13:09.000000000 +0200
+++ vtun-3.0.1/configure.in	2007-10-13 19:14:40.000000000 +0200
@@ -13,25 +13,25 @@
 
 dnl Shapper support
 AC_ARG_ENABLE(shaper,
-   --disable-shaper    	   Don not compile shaper module,
+   --disable-shaper    	   Do not compile shaper module,
    SHAPER=$enableval,
    SHAPER=yes
 )
 dnl Encryption support
 AC_ARG_ENABLE(ssl,
-   --disable-ssl     	   Don not compile encryption module,
+   --disable-ssl     	   Do not compile encryption module,
    SSL=$enableval,
    SSL=yes
 )
 dnl ZLIB support
 AC_ARG_ENABLE(zlib,
-   --disable-zlib     	   Don not compile ZLIB compression module,
+   --disable-zlib     	   Do not compile ZLIB compression module,
    ZLIB=$enableval,
    ZLIB=yes
 )
 dnl LZO support
 AC_ARG_ENABLE(lzo,
-   --disable-lzo     	   Don not compile LZO compression module,
+   --disable-lzo     	   Do not compile LZO compression module,
    LZO=$enableval,
    LZO=yes
 )
